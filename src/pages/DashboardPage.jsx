// DashboardPage.jsx
// This component represents the main Dashboard page of RenewTrack.

function DashboardPage() {
  return (
    <main>
      {/* Page heading */}
      <h1>Dashboard</h1>

      {/* Temporary content.
          Later we will replace this with contract statistics
          and upcoming renewal information. */}
      <p>Welcome to the RenewTrack Dashboard.</p>
    </main>
  );
}

// Export this component so React Router can use it in App.jsx.
export default DashboardPage;